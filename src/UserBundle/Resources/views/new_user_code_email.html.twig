{% set clubName = user.club.name %}
English version below
---------------------

Hei, {{ user.firstName }}.

En admin fra {{ clubName }} har opprettet en bruker for deg.
Følg lenken under for å fullføre registreringen.
{{ app.request.getSchemeAndHttpHost() ~ path('registration_new_user_code', {'code': code}) }}

Mvh. {{ clubName }}
-------------------------------------------------------------------

Hi, {{ user.firstName }}.

An admin from {{ clubName }} has created an user account for you.
To complete the registration please follow the link below.
{{ app.request.getSchemeAndHttpHost() ~ path('registration_new_user_code', {'code': code}) }}

Regards, {{ clubName }}
