<div class="row">
    <div class="col-md-6">
        <form class="enter-click-button">
            <div class="form-group">
                <label class="control-label required" for="{{ idString }}" >{{ label }}</label>
                <input type="text" id="{{ idString }}Content" required="required" class="form-control"
                       value="{{ get_content(idString) }}">
            </div>

            <button type="button" class="btn btn-primary btn-sm btn-flat" id="{{ buttonId }}">{{ buttonLabel }}</button>
        </form>
    </div>
</div>