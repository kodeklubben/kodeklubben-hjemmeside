{% extends 'base/control_panel/admin_base.html.twig' %}

{% block body %}
    <div class="content">
        <div class="row">
            <div class="col-xs-12 col-sm-10 col-md-8 col-lg-6 col-sm-offset-1 col-md-offset-2 col-lg-offset-3">
                <h3>Nytt Barn til {{ user.fullName }}</h3>
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary onclick-disable">Lagre</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
